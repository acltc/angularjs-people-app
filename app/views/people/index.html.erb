<h1>Interesting People</h1>
<div ng-app="app">
  <div ng-controller="peopleCtrl">
    <div ng-repeat="person in people">
      <h3>{{ person.name }}</h3>
      <p>{{ person.details }}</p>
    </div>
  </div>
</div>
